<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/backgroundColor">
    <TextView
        android:id="@+id/tv_password_reset_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="이메일을 입력해주세요."
        android:textSize="25dp"
        android:layout_margin="10dp"
        />
    <EditText
        android:id="@+id/et_password_reset_enter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="이메일을 입력하세요."
        android:textSize="20dp"
        android:layout_margin="10dp"
        android:layout_below="@+id/tv_password_reset_title"/>
    <Button
        android:id="@+id/btn_password_reset_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="취소"
        android:layout_below="@+id/et_password_reset_enter"
        android:layout_toLeftOf="@+id/btn_password_reset_check"
        />
    <Button
        android:id="@+id/btn_password_reset_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="확인"
        android:layout_marginLeft="10dp"
        android:layout_below="@+id/et_password_reset_enter"
        android:layout_alignParentEnd="true"/>
</RelativeLayout>